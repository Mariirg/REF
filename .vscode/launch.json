{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Iniciar Servidor",
            "program": "${workspaceFolder}/server.js",
            "env": {
                "PORT": "3000"
            }
        }
    ]
}
